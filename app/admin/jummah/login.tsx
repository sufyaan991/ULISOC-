"use client";
import {useState} from "react";
import {Eye,EyeOff} from "lucide-react";
export function JummahLogin(){const[password,setPassword]=useState("");const[showPassword,setShowPassword]=useState(false);const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError("");const response=await fetch("/api/admin/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({password})});const data=await response.json();setLoading(false);if(!response.ok){setError(data.error??"Could not sign in.");return}window.location.reload()}return <main className="admin-shell"><section className="login-panel"><a className="back" href="/">← Back to website</a><p className="eyebrow">Committee access</p><h1 className="display">COMMITTEE<br/><span>PORTAL</span></h1><form onSubmit={submit}><label>Committee password<span className="password-field"><input type={showPassword?"text":"password"} autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} autoFocus required/><button className="password-toggle" type="button" onClick={()=>setShowPassword(value=>!value)} aria-label={showPassword?"Hide password":"Show password"} aria-pressed={showPassword}>{showPassword?<EyeOff aria-hidden="true"/>:<Eye aria-hidden="true"/>}</button></span></label><button className="login-submit" disabled={loading}>{loading?"Checking…":"Open portal →"}</button>{error&&<p className="login-error" role="alert">{error}</p>}</form></section><style jsx>{`.login-panel{width:min(560px,calc(100% - 32px));margin:auto;padding:60px 0}.back{display:inline-block;margin-bottom:70px;color:#aaa;font-weight:700}.login-panel h1{font-size:clamp(72px,13vw,120px);margin:14px 0 42px}.login-panel h1 span{color:#b10008}.login-panel form{border:1px solid var(--border);padding:26px;background:#100e0e;display:grid;gap:18px}.login-panel label{display:grid;gap:9px;color:#aaa;text-transform:uppercase;font-size:12px;font-weight:800;letter-spacing:.08em}.password-field{position:relative;display:block}.login-panel input{border:1px solid #3a3030;background:#090808;color:#fff;padding:15px 54px 15px 15px;font-size:18px;width:100%}.password-toggle{position:absolute;right:7px;top:50%;transform:translateY(-50%);display:grid;place-items:center;border:0;background:transparent;color:#aaa;padding:9px;cursor:pointer}.password-toggle:hover,.password-toggle:focus-visible{color:#fff}.password-toggle :global(svg){width:22px;height:22px}.login-submit{border:0;background:#b10008;color:#fff;padding:16px;text-transform:uppercase;font-weight:900;font-size:15px;cursor:pointer}.login-submit:disabled{opacity:.6}.login-error{margin:0;color:#ff858a;font-weight:700}`}</style></main>}
